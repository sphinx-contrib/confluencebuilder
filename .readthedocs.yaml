# readthedocs configuration script
version: 2

sphinx:
  builder: dirhtml
  configuration: doc/conf.py

python:
  install:
    - method: pip
      path: .
    - requirements: doc/requirements.txt

formats: all
