This section contains references to the [something|#term-something] and [else|#term-else] glossary entries.

It also contains the multiple references [multiple|#term-multiple] and [entries|#term-entries] to a single glossary entry plus the nowhere that is a mistake and references a non&#45;existent glossary entry.

They should be converted to references.

h1. glossary

{anchor:term-something}
something
bq. Don’t care what this is, providing it exists.

{anchor:term-else}
else
bq. And this must too.

{anchor:term-multiple}
multiple
{anchor:term-entries}
entries
bq. For a single glossary entry.
